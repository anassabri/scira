import { Icon, IconProps } from '@hugeicons/react-native-pro';

export const LineIcon = (props: IconProps) => (
  <Icon {...props} name="line" />
);

export const PathIcon = (props: IconProps) => (
  <Icon {...props} name="path" />
);